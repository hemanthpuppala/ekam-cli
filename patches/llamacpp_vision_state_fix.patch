--- a/tools/mtmd/mtmd.cpp
+++ b/tools/mtmd/mtmd.cpp
@@ -784,6 +784,8 @@ int32_t mtmd_encode_chunk(mtmd_context * ctx, const mtmd_input_chunk * chunk) {
             LOG_ERR("%s: model does not support audio input\n", __func__);
             return 1;
         }
         int n_mmproj_embd = ctx->n_embd_text;
+        // FIX: Clear stale embeddings before resize to prevent buffer corruption
+        ctx->image_embd_v.clear();
         ctx->image_embd_v.resize(chunk->tokens_audio->n_tokens * n_mmproj_embd);
         bool ok = clip_image_batch_encode(
             ctx->ctx_a,
@@ -804,6 +806,8 @@ int32_t mtmd_encode(mtmd_context * ctx, const mtmd_image_tokens * image_tokens)
         return 1;
     }
     int n_mmproj_embd = clip_n_mmproj_embd(ctx_clip);
+    // FIX: Clear stale embeddings before resize to prevent buffer corruption
+    ctx->image_embd_v.clear();
     ctx->image_embd_v.resize(image_tokens->n_tokens() * n_mmproj_embd);
     bool ok = false;

